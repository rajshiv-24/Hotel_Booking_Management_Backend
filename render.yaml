services:
  - type: web
    name: hotel-booking-backend
    runtime: java
    buildCommand: ./mvnw package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 17